spring.application.name=dsd
server.servlet.context-path=/api/v1

# MySQL configuration
spring.datasource.url=jdbc:mysql://localhost:3306/dsd_gaming
spring.datasource.username=root
spring.datasource.password=root

# JPA configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

#Gmail Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=riteshkusingh27@gmail.com
spring.mail.password=wkvs coas brbt fumk

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000



#jwt configuration
jwt.secret.key=uWo3NfkeHma1yGkIPNQK0TXdROQVWlf2WsDjeF7c1ZM=
#// 15 minutes in milliseconds
jwt.access.expiration=900000
#  // 7 days in milliseconds    
jwt.refresh.expiration=604800000


#Cloudflare R2 storage
cloudflare.r2.access-key-id=${R2_ACCESS_KEY}
cloudflare.r2.secret-access-key=${R2_SECRET_KEY}
cloudflare.r2.bucket-name=${R2_BUCKET}
cloudflare.r2.public-url=${R2_PUBLIC_URL}
cloudflare.r2.endpoint-url=${R2_ENDPOINT_URL}

# Multipart upload limits (helps prevent OutOfMemoryError on large payloads)
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=6MB

